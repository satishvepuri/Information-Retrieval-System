# Phase 3 — Retrieval and Evaluation

Ranks documents using TF-IDF and cosine similarity, processes TREC-style topics, and evaluates retrieval quality with precision and recall.

```bash
g++ -std=c++17 final.cpp -o retrieval
./retrieval
```

Generated ranking and evaluation files are stored in `results/`.
