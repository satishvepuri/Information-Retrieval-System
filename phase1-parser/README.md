# Phase 1 — Text Parser

Parses TREC-style documents, normalizes text, removes stopwords, applies stemming, and constructs term/document dictionaries.

```bash
g++ -std=c++17 parser.cpp -o parser
./parser
```

Place the local document corpus in `ft911/` before running.
